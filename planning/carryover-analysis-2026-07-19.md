# Carryover analysis — 2026-07-19

**Context:** Last standup in repo: 2026-06-19. No Denis Slack replies captured in hq.db since 2026-06-10. Items below were on that standup’s *Today* list or remain open blockers — **no Denis-side progress** in git or DB.

---

## 1. PO apply method + publish channels (P1)

**Evidence:** `jobs/product-owner` body length **0**; `business/jobs/product-owner.md` missing; blocker `input-blocker-po-apply-method-publish-channels` still `in_progress`.

**Top 3 steps**

1. **Denis:** Pick apply method + first channel — `PO apply: form URL — publish first on: LinkedIn` (see `planning/po-apply-decision-brief.md`).
2. **Denis:** Approve `planning/po-jd-expanded-draft-2026-06-19.md` OR paste official JD.
3. **Agent (after 1–2):** Commit JD, careers scaffold in `web/`, LinkedIn announcement draft.

**Agent help today:** Decision briefs unchanged; JD draft ready — blocked only on Denis input.

---

## 2. Office Plovdiv — visit shortlist (P2)

**Evidence:** No Denis office visit picks in DB; shortlist doc from 2026-06-19 never updated with confirmations.

**Top 3 steps**

1. **Denis:** Confirm picks A/B/C from `planning/office-shortlist-refresh-2026-07-19.md`.
2. **Denis:** Send landlord outreach (template: `planning/office-landlord-outreach-2026-06-15.md`).
3. **Agent (after picks):** Lease comparison table + visit checklist in HQ.

**Agent help today:** Re-scraped **1295** listings; Kapana 11108782 still #1; picks A/B/C re-validated; new training option 11237117 noted.

---

## 3. HQ Inbox — automations + social (P2)

**Evidence:** 4 `approve` automations + 3 social drafts still `open` in actions collection.

**Top 3 steps**

1. **Denis:** Approve social copy — `approve: linkedin-page-bio`, `approve: linkedin-first-post`, `approve: x-bio-and-first-post`.
2. **Denis:** Green-light automations in HQ Inbox (`standup`, `competitor: SoftUni AI`, `plan update positioning`, `draft: …`).
3. **Agent (after approvals):** Enable scheduled runs; Buffer/publish path only after explicit publish automation approval.

**Agent help today:** Drafts exist in `social` collection — bios reference “URL TBD” until landing/waitlist path chosen.

---

## 4. Brand sprint days 1–4 (P1 actions, sprint ended 2026-06-22)

**Evidence:** Four `input` actions in `#vibe-business` still `in_progress`; `business/messaging` body empty; no `brand day1`–`day4` replies in DB.

**Top 3 steps**

1. **Denis:** Complete day 1 in ~15 min using `planning/brand-day1-starter-2026-07-19.md` (ICP + anti-audience + secret sauce + ops model).
2. **Denis:** Batch days 2–4 or defer with explicit “defer brand sprint” so PM stops surfacing.
3. **Agent (after day 1):** Merge into `business/messaging` + unlock social CTA copy.

**Agent help today:** Day 1 starter with pre-filled options from `planning/research-brand-positioning-2026-06-19.md`.

---

## 5. Landing page vs presentation deck (P2)

**Evidence:** Both Denis TODOs `in_progress` since 2026-05-29; `web/` is HQ dashboard only; speaker notes exist; no landing scaffold.

**Top 3 steps**

1. **Denis:** Pick path — `deck-first` | `landing-first` | `hybrid` (see `planning/landing-vs-deck-decision-brief.md`).
2. **Denis:** Pick CTA — waitlist | LinkedIn DM | early bird (needs price/seats if early bird).
3. **Agent (after path):** Deck export OR `web/` landing scaffold + deploy checklist.

**Agent help today:** Brief unchanged; hybrid one-pager suggested if Denis wants PO publish + waitlist in parallel.

---

## 6. Create social accounts (P2)

**Blocked by:** Social draft approval + path/CTA (waitlist URL TBD in bios).

**Top 3 steps:** Approve bios → create LinkedIn/X pages → paste handles back to HQ.
