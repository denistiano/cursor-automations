# HQ Inbox — what each approval unlocks (2026-06-17)

**Status:** 7 items still `open` — no Denis replies since 2026-06-10 (day 7).  
**Where:** HQ Inbox → copy each `slack_reply` into `#vibe-standup` or `#vibe-social`.

---

## Automations (4)

| Action | Slack reply | Unlocks |
|--------|-------------|---------|
| `approve-automation-morning-standup-prep` | `standup` | Daily PM standup cron (already running via Cursor — marks action done) |
| `approve-automation-competitor-research` | `competitor: SoftUni AI` | Weekly competitor batch + `#vibe-research` trigger |
| `approve-automation-business-plan-assistant` | `plan update positioning` | Monthly plan vs reality in `#vibe-business` |
| `approve-automation-social-content-draft-only` | `draft: 2 linkedin posts about Cursor automations` | Wednesday social draft batch in `#vibe-social` |

**After Slack reply:** Denis still enables each automation in Cursor UI (`tasks/pm-agent-drafts-build` — Automations 02–04).

---

## Social drafts (3)

| Action | Slack reply | Unlocks |
|--------|-------------|---------|
| `approve-social-linkedin-page-bio` | `approve: linkedin-page-bio` | Denis can create LinkedIn page with approved copy |
| `approve-social-linkedin-first-post` | `approve: linkedin-first-post` | First LinkedIn post ready after profile exists |
| `approve-social-x-bio-and-first-post` | `approve: x-bio-and-first-post` | X profile + first post copy |

**Known gaps in drafts (non-blocking):** waitlist URL `_URL TBD_`; early bird price/dates still backlog.

---

## One-shot batch reply

```
approve: linkedin-page-bio
approve: linkedin-first-post
approve: x-bio-and-first-post
standup
competitor: SoftUni AI
plan update positioning
draft: 2 linkedin posts about Cursor automations
```

---

## Top 3 steps for Denis (HQ Inbox)

1. **Paste batch reply above** (or approve one-by-one in HQ Inbox).
2. **Create LinkedIn + X profiles** after social approvals (task #4).
3. **Enable automations 02–04** in Cursor UI once actions show `done`.
